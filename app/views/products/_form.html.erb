<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <br>
  <br>
  <div class="field">
    <%= f.text_field :name, placeholder: "Product Name", size: 60 %>
  </div>
  <div class="field">
    <%= f.text_area :description, placeholder: "Description", size: "60x4" %>
  </div>
  <div class="field">
    <%= f.text_field :image_url, placeholder: "Image URL", size: 60 %>
  </div>
  <div class="field">
    <%= f.text_field :key, placeholder: "Product Key", size: 60 %>
  </div>
    <div class="field">
    <%= f.text_field :price, placeholder: "Price", size: 60 %>
  </div>
  <div class="actions">
    <%= f.submit "Create Product", class: "form_button" %>
  </div>
  
<% end %>
